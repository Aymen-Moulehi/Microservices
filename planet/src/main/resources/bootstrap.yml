spring:
  profiles:
    active: development
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      enabled: true

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"